#!/usr/bin/expect

set timeout $env(TEST_TIMEOUT_SECS)

spawn qemu-serial

send "\n"

expect {
  eof {
    puts "Failed to get login prompt"
    exit 1
  }
  timeout {
    puts "Failed to get login prompt"
    exit 1
  }
  "$env(QEMU_NAME) login:"
}

puts "Passed"
